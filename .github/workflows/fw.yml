name: First-workflow
on: workflow_dispatch
jobs:
  first-job: 
    runs-on: ubuntu-latest
    steps: 
      - name: print greeting
        run: |
            echo "hello world"
            echo "trying to run multiline command"
      - name: print bye
        run: echo "bye bye"
